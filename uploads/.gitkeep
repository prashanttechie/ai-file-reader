# This file ensures the uploads directory exists in the repository
# The uploads directory is needed for file storage but the uploaded files
# should not be committed to git (they are ignored in .gitignore)
